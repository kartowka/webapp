import { StyleSheet, StatusBar } from 'react-native'
const HomeScreenWrapper = StyleSheet.create({
  background: {
    marginTop: StatusBar.currentHeight,
    flex: 1,
  },
})

export default HomeScreenWrapper
